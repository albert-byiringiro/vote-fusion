type LoaderProps = {
    color: 'blue' | 'orange' | 'purple';
    isLoading: boolean;
    width?: number;
}

const colorStyles = {
    blue: 'bg-blue',
    orange: 'bg-orange',
    purple: 'bg-purple',
}

